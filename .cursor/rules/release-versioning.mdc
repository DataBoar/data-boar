---
description: Release/version guardrails — commit gate vs release gate, ADR-0073 side-channel, no 1.7.5, semver TBD. Situational — semver bumps, VERSIONING, pyproject version.
globs:
  - pyproject.toml
  - docs/VERSIONING.md
  - docs/VERSIONING.pt_BR.md
  - CHANGELOG.md
  - security/version_policy.yaml
  - tests/test_release_version_policy.py
  - docs/releases/**
  - docs/adr/ADR-0072*.md
  - docs/adr/ADR-0073*.md
alwaysApply: false
---

# Release & versioning guardrails (post-#970)

## When this rule attaches

**Situational** — globs above, semver bump / release doc work, or **`@release-versioning.mdc`**. **`release-publish-sequencing.mdc`** covers **tag → GitHub → Docker → Hub** choreography; **this rule** covers **authorization to bump** and **phantom-version prevention**.

## Two gates (ADR-0072 — Proposed)

| | **Commit gate** | **Release gate** |
|---|---|---|
| Question | May this code enter `main`? | May this version become stable? |
| Criterion | `check-all` / CI / pre-commit GREEN | Lab completão (#406) + operator FASE 3 |
| Vocabulary | **Never** call this "gate" alone — **local checks** / **CI** | **Gate** without qualifier = **this** |

**Green commit gate never authorizes removing `-beta`/`-rc` from `pyproject.toml`.**

## Version facts (ADR-0073 — Accepted)

- **`main` today:** **`1.7.4`** on release branch **`release/1.7.4`** (PR **#1024**); ADR-0073 **Accepted**; release gate **#406** closes in the same PR train before merge.
- **#970:** premature stable bump/tag without release gate — corrected by **ADR-0072** + **#406**; **`1.7.4` is not VOID**.
- **Stable semver + tag after gate:** **TBD** (operator HITL post-GA numbering — three segments + suffix only; no fourth segment).
- **`1.7.5` does not exist.** Next dev line: **`1.8.0-beta`** (#772).
- **Maturity octet** (0–127 beta · 128–199 rc · 200–255 release): **`[tool.databoar] maturity_build`** side-channel — **never** in `[project] version` or About. See **`docs/VERSIONING.md`**.

## Agent must not

1. Bump to **stable** (no suffix) while **#406** is OPEN.
2. Invent **`1.7.5`** / **`1.7.5-beta`** targeting.
3. Treat **verifiable** smoke hardening (#831) as **release gate passed**.
4. Tag, GitHub Release, or Docker publish without operator **FASE 3** — pair with **`release-publish-sequencing.mdc`**.

## Machine guard

**`security/version_policy.yaml`** + **`tests/test_release_version_policy.py`** — run via **`check-all`**.

## Canonical refs

- **`docs/VERSIONING.md`** · **ADR-0072** · **ADR-0073** · **#970** · **#772** · **#978**
