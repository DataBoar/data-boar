# Pre-release 1.8.0-beta (git working line — not PyPI / Docker Hub marketing)

**Kind:** **GitHub pre-release** (non-production) when tagged — use **Set as a pre-release** / `gh release create … --prerelease`.

**Working version string:** **`1.8.0-beta`** in `pyproject.toml` and on public surfaces (`--version`, About, User-Agent).

**Maturity octet (ADR-0073):** **`[tool.databoar] maturity_build = 69`** — **new public line** after **`1.7.4`**. Octet **resets** into the beta band (**1–126**) at cut (`604c1b5c`, 2026-08-02, first beta = **`.1`**). **Reconciled 2026-08-18:** **68** `fix`/`feat` commits on `main` since the cut were not advanced in `pyproject.toml` during a heavy sprint — **operational lapse, not a versioning-policy regression**; applied `1 + 68 = 69`. Does **not** continue from **`1.7.4.post12`** / **`.263`**. **`.postN`** does not apply on this pre-release line. **`[project] version` remains `1.8.0-beta`** (no octet in the public string).

**Published customer channels (unchanged until a future stable `1.8.0` ritual):**

| Channel | Still |
| --- | --- |
| **PyPI / Docker Hub / GitHub Latest** | **`1.7.4.post12`** / **`v1.7.4.post12`** (and historical GA **`1.7.4`**) |
| **Hub marketing paste** | No refresh required for **`-beta`** alone ([VERSIONING.md](../VERSIONING.md), release-publish-sequencing) |

---

## Intent

- Open the **`1.8.0`** architecture line on `main` for UAT/QA from **git** (ADR-0073 Decision 5; ADR-0072 pre-release discipline).
- Keep **PyPI / Docker Hub `latest`** on the **`1.7.4`** fix-line until a later **release gate** + **release-ritual** for stable **`1.8.0`**.

---

## Map: public version ↔ maturity_build (new line)

| `[project] version` | `maturity_build` | Notes |
| --- | --- | --- |
| **`1.7.4.post12`** (prior line, published) | **`.263`** | Last PyPI/Hub publish on the **`1.7.4`** line — see [1.7.4.post12.md](1.7.4.post12.md) |
| **`1.8.0-beta`** (cut `604c1b5c`) | **`.1`** | Band reset at new public line |
| **`1.8.0-beta`** (reconciled 2026-08-18) | **`.69`** | `1 + 68` fix/feat since cut — see CHANGELOG §1.8.0-beta |

---

## Highlights carried into this working tree

- Route-matrix collector walks nested `include_router` / FastAPI 0.139+ `_IncludedRouter` (#1426) — Dependabot uv bump can land without snapshot maquillage.
- uv minor/patch lock + `requirements.txt` sync on maintainer branch (#1428; structural Dependabot sync gap → #1419).
- `/logs` auth aligned with RBAC middleware (#1190 / #1418).
- Remediation manifest export CLI (#649 / #1394) and other `main` slices since **`1.7.4.post12`**.
- Native packages on the product GitHub Release (#1408): nfpm deb/rpm/apk/pacman attach beside SBOMs on a `v*` tag; air-gap [NATIVE_PACKAGE_RELEASE.md](../ops/NATIVE_PACKAGE_RELEASE.md). The hand-built recipe-proof `.deb` is not published.

See root **[CHANGELOG.md](../../CHANGELOG.md)** section **`1.8.0-beta`**.

---

## Optional GitHub pre-release (operator only)

After this bump is on `main` and the operator wants a git tag:

```bash
git tag -a v1.8.0-beta -m "v1.8.0-beta: open 1.8.0 development line"
git push origin v1.8.0-beta
gh release create v1.8.0-beta --prerelease --title "v1.8.0-beta (pre-release)" --notes-file docs/releases/1.8.0-beta.md
```

**Do not** move Docker Hub **`latest`** or publish PyPI for **`-beta`** (distribution policy: git-only for external consumers).

---

## Toward `1.8.0-rc` / `1.8.0`

1. Land architecture slices under milestone **v1.8.0** / **v1.8.0-beta**.
2. Promote working version **`1.8.0-beta` → `1.8.0-rc[.N]`** when candidate-ready (ADR-0072).
3. Stable **`1.8.0`** only with release gate + operator FASE 3 + **release-ritual** (tag → GitHub → smoke → Hub/PyPI as applicable).
