# Release 1.7.4.post2 (PyPI publish counter)

**Status:** Pending PyPI upload after merge of PR for Estela onboarding (`--demo`).

**Public line (marketing):** **`1.7.4`** — README, man pages, stakeholder copy unchanged.

**Build / PyPI / About:** **`1.7.4.post2`** — PEP 440 post-release (publish counter per [ADR-0073](../adr/ADR-0073-version-scheme-octet-maturity-and-roadmap.md) § *PyPI dual counters*.

---

## Mandatory map: `postN` ↔ `maturity_build`

Per ADR-0073 (ratified 2026-06-27): without this table, publication and maturity counters diverge without audit trail.

| PyPI / `[project] version` | `maturity_build` (octet) | Notes |
| --- | --- | --- |
| **`1.7.4`** | **`.201`** | GA on PyPI (immutable); release PR **#1024** |
| **`1.7.4.post1`** | **`.202`** | SQL drivers → optional extras; lean core `pip install` ([#1047](https://github.com/FabioLeitao/data-boar/issues/1047)) |
| **`1.7.4.post2`** | **`.204`** | Turnkey **`data-boar --demo`** onboarding ([#1119](https://github.com/FabioLeitao/data-boar/pull/1119)) |

**Divergence note:** Octet **`.203`** may exist on unpublished local builds between post1 and post2; the next **PyPI** pair after **`1.7.4.post1`** is **`1.7.4.post2` ↔ `.204`** per ADR-0073.

---

## Highlights

- **`data-boar --demo`:** Zero-config turnkey demo — synthetic corpus, scan, loopback dashboard on `127.0.0.1:8088` (no `config.yaml` required). Target: Windows operators and first-time `pip install` users (Estela onboarding).
- **Excel report fix:** Sheet title sanitization for openpyxl (slash in praise/controls sheet name).
- **Operator help / docs:** `QUICKSTART.md` Caminho 0, man page, dashboard help surface aligned to `--demo`.

---

## Install

```bash
pip install 'data-boar==1.7.4.post2'
data-boar --demo
```

See [QUICKSTART.md](../QUICKSTART.md) (Caminho 0) and [USAGE.md](../USAGE.md).
